import 'package:flutter/material.dart';

class RepositorySubPage extends StatelessWidget {
  const RepositorySubPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
